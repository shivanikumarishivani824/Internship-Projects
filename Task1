<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Simple Responsive Landing Page</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <header class="site-header">
    <div class="container header-inner">
      <div class="logo">Shivani<span>Elevate lab</span></div>
      <input type="checkbox" id="nav-toggle" class="nav-toggle">
      <label for="nav-toggle" class="nav-toggle-label" aria-hidden="true">
        <span></span>
        <span></span>
        <span></span>
      </label>
      <nav class="site-nav">
        <ul>
          <li><a href="#">Home</a></li>
          <li><a href="#">Features</a></li>
          <li><a href="#">Pricing</a></li>
          <li><a href="#">Contact</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <main>
    <section class="hero">
      <div class="container hero-inner">
        <div class="hero-text">
          <h1>Build beautiful things, faster.</h1>
          <p>Deliver clean, responsive interfaces with a tiny CSS framework and simple components.</p>
          <div class="cta-row">
            <a class="btn primary" href="#">Get Started</a>
            <a class="btn outline" href="#">Learn More</a>
          </div>
        </div>
        <div class="hero-art">
          <div class="mockup">HTML &amp; CSS</div>
        </div>
      </div>
    </section>
  </main>

  <footer class="site-footer">
    <div class="container footer-inner">
      <p>&copy; <span id="year"></span> BrandCo — All rights reserved.</p>
      <div class="social">
        <a href="#" aria-label="Twitter">Twitter</a>
        <a href="https://www.linkedin.com/in/shivanikumari12/" aria-label="linkedin">linkedin</a>
        <a href="#" aria-label="Instagram">Instagram</a>
      </div>
    </div>
  </footer>

  <script>
    document.getElementById('year').textContent = new Date().getFullYear();
  </script>
</body>
</html>


//stylesheet
/* Basic reset and variables */
:root{
  --bg:#f7f8fc;
  --accent:#6c5ce7;
  --muted:#6b7280;
  --white:#ffffff;
  --max-width:1100px;
}
*{box-sizing:border-box;margin:0;padding:0}
html,body{height:100%}
body{font-family:Inter,Segoe UI,Roboto,Arial, sans-serif;background:var(--bg);color:#0f172a;line-height:1.5}
.container{width:90%;max-width:var(--max-width);margin:0 auto}

/* Header */
.site-header{background:transparent;position:sticky;top:0;z-index:10;padding:18px 0}
.header-inner{display:flex;align-items:center;justify-content:space-between}
.logo{font-weight:700;font-size:1.2rem;color:var(--accent)}
.logo span{color:#111}

.site-nav ul{display:flex;gap:20px;list-style:none}
.site-nav a{text-decoration:none;color:inherit;padding:8px 12px;border-radius:6px}
.site-nav a:hover{background:rgba(108,92,231,0.08)}

.nav-toggle{display:none}
.nav-toggle-label{display:none;cursor:pointer;width:32px;height:24px}
.nav-toggle-label span{display:block;height:3px;background:#111;margin:6px 0;border-radius:2px}

/* Hero */
.hero{padding:64px 0}
.hero-inner{display:flex;gap:40px;align-items:center}
.hero-text{flex:1}
.hero-text h1{font-size:2.2rem;margin-bottom:12px}
.hero-text p{color:var(--muted);margin-bottom:20px}
.cta-row{display:flex;gap:12px}
.btn{display:inline-block;padding:10px 18px;border-radius:8px;text-decoration:none}
.btn.primary{background:var(--accent);color:var(--white)}
.btn.outline{border:1px solid rgba(15,23,42,0.08);background:transparent;color:inherit}

.hero-art{flex:1;display:flex;justify-content:center}
.mockup{background:linear-gradient(135deg,#fff 0%, #f1f5f9 100%);padding:48px;border-radius:12px;box-shadow:0 8px 30px rgba(15,23,42,0.08);color:var(--muted)}

/* Footer */
.site-footer{padding:20px 0;margin-top:40px;background:transparent}
.footer-inner{display:flex;justify-content:space-between;align-items:center;color:var(--muted);gap:12px}
.social a{margin-left:12px;text-decoration:none;color:inherit}

/* Responsive rules */
@media (max-width: 900px){
  .hero-inner{flex-direction:column;text-align:center}
  .hero-text h1{font-size:1.8rem}
  .hero-art{order:-1}
}

@media (max-width: 768px){
  .site-nav{position:absolute;top:68px;left:0;right:0;background:var(--white);padding:14px 0;box-shadow:0 8px 24px rgba(2,6,23,0.06)}
  .site-nav ul{flex-direction:column;align-items:center}
  .site-nav a{display:block;padding:10px 16px}
  .nav-toggle-label{display:block}
  .site-nav{display:none}
  .nav-toggle:checked + .nav-toggle-label + .site-nav{display:block}
}
